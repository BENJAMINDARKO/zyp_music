"  Widget _buildQueueView(BuildContext context, PlayerProvider player) {\n    return Consumer<PlayerProvider>(\n      builder: (context, provider, _) {\n        final queue = provider.queue;\n        final currentIndex = provider.currentIndex;\n        final onSurface = Theme.of(context).colorScheme.onSurface;\n        final primary = Theme.of(context).colorScheme.primary;\n\n        // Split queue into history, current, and upcoming\n        final historyTracks = currentIndex > 0 ? queue.sublist(0, currentIndex) : <Track>[];\n        final currentTrack = (currentIndex >= 0 && currentIndex < queue.length) ? queue[currentIndex] : null;\n        final upcomingTracks = (currentIndex + 1 < queue.length) ? queue.sublist(currentIndex + 1) : <Track>[];\n\n        return Container(\n          margin: const EdgeInsets.symmetric(horizontal: 16),\n          child: CustomScrollView(\n            slivers: [\n              // ── History Header ──\n              if (historyTracks.isNotEmpty)\n                SliverToBoxAdapter(\n                  child: Padding(\n                    padding: const EdgeInsets.fromLTRB(16, 12, 16, 4),\n                    child: Row(\n                      mainAxisAlignment: MainAxisAlignment.spaceBetween,\n                      children: [\n                        Text(\n                          'History',\n                          style: TextStyle(\n                            fontSize: 20,\n                            fontWeight: FontWeight.bold,\n                            color: onSurface,\n                          ),\n                        ),\n                        GestureDetector(\n                          onTap: () {\n                            // Clear history by removing all tracks before currentIndex\n                            for (int i = 0; i < currentIndex; i++) {\n                              provider.removeFromQueue(0);\n                            }\n                          },\n                          child: Text(\n                            'Clear',\n  
<truncated 13549 bytes>